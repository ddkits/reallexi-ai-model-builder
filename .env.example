# Reallexi public launcher image overrides.
# Login to the private registry first when images are private:
#   docker login ghcr.io

REALLEXI_BACKEND_IMAGE=ghcr.io/ddkits/ai-model-builder-backend:1.0.0
REALLEXI_FRONTEND_IMAGE=ghcr.io/ddkits/ai-model-builder-frontend:1.0.0
